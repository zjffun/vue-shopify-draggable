export const defaultTouchEventOptions = {
  touches: [
    {
      pageX: 0,
      pageY: 0,
    },
  ],
};

export const defaultMouseEventOptions = {
  button: 0,
  clientX: 0,
  clientY: 0,
  pageX: 0,
  pageY: 0,
};

export const DRAG_DELAY = 100;

export const REQUEST_ANIMATION_FRAME_DELAY = 16;
